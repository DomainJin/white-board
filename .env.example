# Copy file này thành .env rồi chỉnh lại

# Domain/IP của server (dùng khi deploy production)
CLIENT_URL=http://localhost

# Bắt buộc đổi khi production!
JWT_SECRET=change-me-to-a-long-random-string-minimum-64-characters
